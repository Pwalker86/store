<h1>Editing product</h1>

<%# <%= turbo_frame_tag "edit_product_#{@product.id}" do %1> %>
<%= render "form", product: @product %>
<%# <% end %1> %>

<br>

<div>
  <%= link_to "Show this product", @product %> |
  <%= link_to "Back to products", products_path %>
</div>
