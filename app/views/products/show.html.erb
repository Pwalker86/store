<% turbo_frame_tag "show_product_#{@product.id}" do %>
  <%= render @product %>
<% end %>

<div>
  <%= link_to "Back to products", products_path %>
  <% if admin_signed_in? %>
    <%= link_to "Edit this product", edit_product_path(@product) %> |
    <%= button_to "Destroy this product", @product, method: :delete %>
  <% end %>
</div>
