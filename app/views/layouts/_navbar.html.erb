<div class="Navbar__Container">
  <div class="col-left">

    <div class="Navbar__LoggedInAs">
      Logged in as:
      <%= display_username %>
    </div>
    <div>
      <%= link_to "View All Orders", orders_path %>
      <%#= link_to "View Products", store_products_path(@store) %>
    </div>
  </div>
  <div class="Navbar__Links col-right">
    <%= link_to "Home", pages_home_path %>
    <%# if @active_user  && !current_admin %>
      <%#= link_to "Current Order: #{@open_order.order_item_totals}",
        @cart,
      title: "View Order" %>
    <%# end %>
    <% if @active_user && @active_user.is_a?(User) %>
      <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete } %>
    <% end %>
  </div>
</div>
